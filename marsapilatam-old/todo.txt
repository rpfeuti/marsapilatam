standardize requested fields on each service, create a dict in the config with one column for each service specifying which field each service gets

reimplement KRR

todos os services tem que voltar um dataframe. Alguns ainda estao voltando dict. Gradualmente vou atualizando.

nos param builder, fazer de uma forma que as entradas sejam optional para nao ficar fazendo gambiarra nos tipos, tipo colocar "" num float ou data

trocar todas as dtas que estao em STR para DATA, agora parece que api aceita datas

os servicos nao tem tratamento de erros, as excessoes sao tratanas no-gui via try-catch e no streamlit ele mesmo mostra

TYPING
standardize return types of services, using types when possible

SWAP CUSTOM
payfrequency not going to back-end on swap custom

CURVES
add multi select on curve id 